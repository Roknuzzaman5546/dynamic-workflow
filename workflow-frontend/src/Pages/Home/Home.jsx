import Banner from "./Banner";

const Home = () => {

  return (
    <div>
      {/* <h1>Welcome, {user.name}</h1>
          <button onClick={logout}>Logout</button> */}
      <Banner />
    </div>
  );
};

export default Home;
